/* The app's entry point is the `@main` App inside the library this target links, exactly
 * as it is in the builder package xtool generates for iOS. A target still needs a source
 * file, so this is it.
 */
